coverage configure -2stepexclusion off
# Read the base exclusions from apply_exclusions.do but manually, to avoid running its 'quit' or 'save'
# We will just write a script that does all exclusions + our new functional exclusions!
